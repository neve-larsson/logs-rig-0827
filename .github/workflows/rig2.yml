name: rig2
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: prove-presence
        run: |
          echo "SENTINEL-LEN: ${#RIG_SENTINEL}"
          echo "SENTINEL-FIRST4: ${RIG_SENTINEL:0:4}"
          echo "SENTINEL-FULL: $RIG_SENTINEL"
